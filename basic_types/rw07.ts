 let num = 'dkdsvsdklj329-';

export function createNewError(msg: string) {
    throw new Error(msg);
}

 if (typeof num !== 'number') throw new Error("TypeError");

 if (typeof num !== 'number') createNewError("Erro de tipo");

enum Veiculos {
    CARRO,
    MOTO,
    CAMINHÃO
}

